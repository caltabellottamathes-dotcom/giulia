import React from "react";

/**
 * AmbientBloom — a small, floating, drifting #b2bfc7 blob in the bottom-left
 * corner. Purely decorative "living canvas" light; not interactive.
 */
export default function AmbientBloom() {
  return <div className="ambient-bloom" aria-hidden="true" />;
}